
import { uiModules } from 'ui/modules';

const app = uiModules.get('apps/arcanna');

app.factory('selectedIndexList', () => {
  return [];
});

app.factory('indexFieldMappings', () => {
  return {};
})

app.factory('feedbackJobInformation', () => {
  return {};
});
